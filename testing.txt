#include <stdio.h>;
#include <math.h>;
const int MAX = 100;
const float PI = 3.14;
var int x, y, z;
var float radius, area;

function computeArea;
var int a, b;
newb
    x := 10;
    y := 20;
    z := "x" + "y";
    cout << "z";
    if ("x" > "y") z := "x"
    else z := "y" - "x";
    while (z > 0) newb
        z := "z" - 1;
    endb;
    repeat
        radius := "radius" + 1;
        area := "PI" * "radius" * "radius";
    until "radius" > 10;
endb;

function main;
newb
    call compute_area;
    cout << area;
endb;

exit
